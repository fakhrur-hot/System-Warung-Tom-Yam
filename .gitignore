# ── Secrets / local config (NEVER commit) ──────────────────────────────
local.properties
keystore.properties
*.jks
*.keystore
.env
.env.local
.env.*.local
# .env.example templates ARE committed (they hold placeholders only)
supabase_k*
*.supabase-keys*
credentials.txt
secrets.txt

# ── Deployment-specific (per-café) — keep the product repo generic ─────
# The actual café's Cloudflare Pages config is local-only; commit only the .example.
website/wrangler.toml

# ── Android / Gradle ───────────────────────────────────────────────────
*.apk
*.aab
.gradle/
build/
captures/
.cxx/
*.iml

# ── Web (website/) ─────────────────────────────────────────────────────
node_modules/
dist/
.vite/
*.log
npm-debug.log*
yarn-error.log*

# ── TypeScript build info ──────────────────────────────────────────────
*.tsbuildinfo

# ── IDE / OS ───────────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# ── Internal-only material (proprietary project — see LICENSE) ────────────────
# Kept in the working tree, deliberately out of the published repo: these are the
# design record and operational runbooks, not shipping artefacts. Removed from
# tracking on 2026-07-31 when the project licence was settled as proprietary.
# NOTE: removal stops future publication only — earlier revisions remain in the
# pushed history and are still retrievable. Verified at the time of removal that
# none of these files ever contained real credentials.
.kiro/specs/
docs/
DEPLOYMENT.md
*_planner.md
# The original product spec — same internal-only class as .kiro/specs/ above. Untracked 2026-07-31.
specs/
# Dev screenshots taken while verifying on-device behaviour; not shipping artefacts.
screenshot*.png
