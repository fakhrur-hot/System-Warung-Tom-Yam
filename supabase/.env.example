# Copy to .env.local (git-ignored) for local Edge Function dev, and set the SAME values as
# Edge Function secrets in production (see DEPLOYMENT.md). All server-side — never commit.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxx
ROTATING_KEY_SECRET=hex-32-bytes-from-openssl-rand
BREVO_API_KEY=xkeysib-xxxxxxxx
