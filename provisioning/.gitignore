# Generated at build time by scripts/bundle-migrations.mjs and scripts/inline-functions.mjs from
# supabase/migrations and supabase/functions — regenerated by `npm run generate` (part of
# `npm run build`), not committed, so it never drifts from the source it was derived from.
functions/_generated/
